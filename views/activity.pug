extends layout

block content
  h1 Activity Page

  div
    form(action = "" method = "POST")
    p Username
      input(type = "text" placeholder ="")

    p Password
      input(type = "text" placeholder ="")

    p Submit
      input(type = "submit"  placeholder ="")
